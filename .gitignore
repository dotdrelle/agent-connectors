node_modules/
dist/
coverage/
.env
.env.*
!.env.example
# Application OAuth ecrite dans le contexte de build par build-local.sh /
# build-and-push.sh, puis retiree. Ne doit jamais etre commitee.
.oauth-client.json
.DS_Store
*.log
*.tsbuildinfo

# Workspace LLM configuration: carries llm.apiKey and mcp.accessKey.
.wikirc*

# Artefacts FUSE : fichier remplacé alors qu'un processus le tenait ouvert.
.fuse_hidden*
